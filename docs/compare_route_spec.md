# Compare Route Spec

## 목표
compare 질의를 multi-page/general lookup과 분리한다.

## 규칙
- `비교`, `차이`, `vs`, `versus`는 compare 우선
- `와`, `과`, `및`, `함께`는 다중 대상 힌트로 사용하되, compare cue와 함께 있으면 compare 우선
- compare route는 최소 2 distinct evidence를 필요로 한다.

## 후속 과제
- pair ranking
- compare-specific answer policy
